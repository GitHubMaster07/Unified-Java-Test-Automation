Base URL for the application under test (The Internet by Herokuapp)

base.url=https://the-internet.herokuapp.com

Browser selection (e.g., chrome, firefox)

browser=chrome

Timeout settings in seconds

implicit.wait.timeout=5
page.load.timeout=10